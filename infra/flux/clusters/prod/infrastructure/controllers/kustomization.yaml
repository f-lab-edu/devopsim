apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secrets.yaml
  - karpenter.yaml
  - reloader.yaml
  - kube-prometheus-stack.yaml
  - loki.yaml
  - alloy.yaml
  # Gateway API CRDs는 Traefik / cert-manager의 의존성 → controller보다 먼저 적용.
  - gateway-api-crds.yaml
